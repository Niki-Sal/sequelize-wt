<!-- we have access to users property -->
<a href ="/coders/new">Add a new coder</a>
<% for (let i=0; i < users.length; i++) { %>
    <p><%= users[i].firstName %> is <%= users[i].age %> years old</p>

<% } %>

